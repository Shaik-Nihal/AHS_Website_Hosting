document.addEventListener("DOMContentLoaded",function(){const counters=document.querySelectorAll(".counter");const duration=2e3;let started=false;const startCounters=()=>{const startTime=performance.now();const updateCounters=()=>{const elapsedTime=performance.now()-startTime;const progress=Math.min(elapsedTime/duration,1);counters.forEach(counter=>{const target=+counter.getAttribute("data-target");const value=Math.floor(target*progress);counter.innerText=value+(progress===1?"+":"")});if(progress<1){requestAnimationFrame(updateCounters)}};updateCounters()};const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting&&!started){started=true;startCounters()}})});const statsSection=document.querySelector(".apollo-stats");if(statsSection){observer.observe(statsSection)}});