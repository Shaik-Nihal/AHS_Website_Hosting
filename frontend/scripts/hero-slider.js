let slides=[];let dots=[];let currentSlide=0;const prevBtn=document.getElementById("prev-btn");const nextBtn=document.getElementById("next-btn");const slideInterval=5e3;let slideTimer;function initializeSlider(dynamicSlides,dynamicDots){slides=dynamicSlides;dots=dynamicDots;currentSlide=0;if(slides.length===0){console.warn("No slides to initialize for the slider.");if(document.querySelector(".slider-controls")){document.querySelector(".slider-controls").style.display="none"}return}if(slides.length>0&&!slides[0].classList.contains("active")){slides[0].classList.add("active")}if(dots.length>0&&!dots[0].classList.contains("active")){dots[0].classList.add("active")}function showSlide(n){slides.forEach((slide,index)=>{slide.classList.remove("active");if(dots[index])dots[index].classList.remove("active")});currentSlide=(n+slides.length)%slides.length;slides[currentSlide].classList.add("active");if(dots[currentSlide])dots[currentSlide].classList.add("active");clearInterval(slideTimer);slideTimer=setInterval(nextSlide,slideInterval);setHeroBackground()}function nextSlide(){showSlide(currentSlide+1)}function prevSlide(){showSlide(currentSlide-1)}if(prevBtn&&nextBtn){prevBtn.removeEventListener("click",prevSlide);nextBtn.removeEventListener("click",nextSlide);prevBtn.addEventListener("click",prevSlide);nextBtn.addEventListener("click",nextSlide)}dots.forEach((dot,index)=>{dot.removeEventListener("click",()=>showSlide(index));dot.addEventListener("click",()=>{showSlide(index)})});setHeroBackground();clearInterval(slideTimer);slideTimer=setInterval(nextSlide,slideInterval)}function setHeroBackground(){const hero=document.querySelector(".hero");const activeSlideImg=document.querySelector("#hero-slider .slide.active img");if(hero&&activeSlideImg){if(window.innerWidth<=768){hero.style.backgroundImage=`url('${activeSlideImg.src}')`;hero.style.backgroundSize="cover";hero.style.backgroundPosition="center";hero.style.backgroundRepeat="no-repeat"}else{hero.style.backgroundImage=""}}}window.addEventListener("resize",setHeroBackground);